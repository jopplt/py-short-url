[pytest]
addopts = -ra -q

pythonpath = app

testpaths =
    tests

markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    serial

filterwarnings =
    ignore:.+:DeprecationWarning

env =
    REDIS_HOST=test
    REDIS_PORT=6379
    REDIS_PASSWORD=secret